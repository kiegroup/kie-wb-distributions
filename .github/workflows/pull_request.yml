name: Disk Space Info

on: [pull_request]

jobs:
  build-chain:
    runs-on: ubuntu-latest
    name: disk space info
    steps:
      - name: Install takari
        run: df -H
                                                                                
