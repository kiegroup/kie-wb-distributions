<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.kie.smoke</groupId>
    <artifactId>kie-wb-smoke-tests</artifactId>
    <version>6.4.0.CR2</version>
  </parent>

  <artifactId>kie-wb-smoke-tests-arq</artifactId>

  <name>KIE (Drools) Workbench Smoke Tests :: Arquillian</name>
  <description>Arquillian Integration smoke tests for KIE Workbench and KIE Drools Workbench</description>

  <properties>
    <!-- JWS 3.0 is tomcat 8.0.18 -->
    <!-- https://access.redhat.com/articles/111723 -->
    <version.org.apache.tomcat>7.0.65</version.org.apache.tomcat>
    <!-- TODO: neither the wildfly 8 or 10 profiles work fully yet -->
    <skipITs>true</skipITs>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.jboss.arquillian.container</groupId>
        <artifactId>arquillian-tomcat-managed-7</artifactId>
        <version>1.0.0.CR7</version>
      </dependency>

      <dependency>
        <groupId>org.apache.tomcat</groupId>
        <artifactId>tomcat</artifactId>
        <version>${version.tomcat7}</version>
        <type>zip</type>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.kie.smoke</groupId>
      <artifactId>kie-wb-smoke-tests-main</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.kie.smoke</groupId>
      <artifactId>kie-wb-smoke-tests-main</artifactId>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>

    <!--  kie -->
    <dependency>
      <groupId>org.kie</groupId>
      <artifactId>kie-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jbpm</groupId>
      <artifactId>jbpm-kie-services</artifactId>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <groupId>org.kie</groupId>
          <artifactId>kie-ci</artifactId>
        </exclusion>
        <exclusion>
          <artifactId>aether-api</artifactId>
          <groupId>org.eclipse.aether</groupId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.kie.remote</groupId>
      <artifactId>kie-remote-client</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.guvnor</groupId>
      <artifactId>guvnor-rest-client</artifactId>
      <scope>test</scope>
    </dependency>

    <!--  general -->
    <dependency>
      <groupId>commons-io</groupId>
      <artifactId>commons-io</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>commons-net</groupId>
      <artifactId>commons-net</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- utilities -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- REST -->
    <dependency>
      <groupId>org.jboss.resteasy</groupId>
      <artifactId>jaxrs-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.resteasy</groupId>
      <artifactId>resteasy-jackson-provider</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.resteasy</groupId>
      <artifactId>resteasy-jaxb-provider</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- arquillian -->
    <dependency>
      <groupId>org.jboss.arquillian.junit</groupId>
      <artifactId>arquillian-junit-container</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap</groupId>
      <artifactId>shrinkwrap-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap.resolver</groupId>
      <artifactId>shrinkwrap-resolver-api-maven</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap.resolver</groupId>
      <artifactId>shrinkwrap-resolver-spi</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.jboss.shrinkwrap.resolver</groupId>
      <artifactId>shrinkwrap-resolver-impl-maven</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>false</filtering>
      </testResource>
      <testResource>
        <directory>src/test/filtered-resources</directory>
        <filtering>true</filtering>
      </testResource>
      <!-- Going outside the module directory is not good practice, but in this case we are not relying on
           build artifacts, but rather comitted resources, which are supposed to be always there. This also
           means we can have the resources only at one place (the other module) without the need to duplicate them. -->
      <testResource>
        <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources</directory>
        <filtering>false</filtering>
      </testResource>
      <testResource>
        <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/filtered-resources</directory>
        <filtering>true</filtering>
      </testResource>
    </testResources>

    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <configuration>
            <filesets>
              <fileset>
                <directory>${basedir}</directory>
                <includes>
                  <include>repositories/</include>
                  <include>.niogit/</include>
                  <include>.index/</include>
                </includes>
              </fileset>
            </filesets>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <profiles>
    <profile>
      <id>kie-drools-wb</id>
      <properties>
        <deployable.groupId>org.kie</deployable.groupId>
        <deployable.artifactId>kie-drools-wb-distribution-wars</deployable.artifactId>
        <deployable.context>kie-drools-wb</deployable.context>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-failsafe-plugin</artifactId>
              <configuration>
                <groups>KieDroolsWbSmoke</groups>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>

    <profile>
      <id>kie-wb</id>
      <activation>
        <property>
          <name>full</name>
        </property>
      </activation>
      <properties>
        <deployable.groupId>org.kie</deployable.groupId>
        <deployable.artifactId>kie-wb-distribution-wars</deployable.artifactId>
        <deployable.context>kie-wb</deployable.context>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-failsafe-plugin</artifactId>
              <configuration>
                <groups>KieWbSmoke</groups>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>

    <profile>
      <id>arquillian-eap64x</id>
      <properties>
        <override.version.org.jboss.as>7.5.4.Final-redhat-3</override.version.org.jboss.as>
        <version.org.jboss.as.dist>7.5.4.Final-redhat-3</version.org.jboss.as.dist>
        <jboss.server.name>jboss-eap-6.4</jboss.server.name>
        <deployable.classifier>${eap64.deployable.classifier}</deployable.classifier>

        <arquillian.launch>eap6</arquillian.launch>
      </properties>

      <dependencies>
        <!--  wars -->
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-wb-distribution-wars</artifactId>
          <classifier>${eap64.deployable.classifier}</classifier>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-drools-wb-distribution-wars</artifactId>
          <classifier>${eap64.deployable.classifier}</classifier>
          <type>war</type>
        </dependency>

        <!--  arquilian -->
        <dependency>
          <groupId>org.jboss.as</groupId>
          <artifactId>jboss-as-arquillian-container-managed</artifactId>
          <scope>test</scope>
        </dependency>

        <!-- jboss as/eap -->
        <dependency>
          <groupId>org.jboss.as</groupId>
          <artifactId>jboss-as-dist</artifactId>
          <type>zip</type>
        </dependency>
      </dependencies>

      <dependencyManagement>
        <dependencies>
          <dependency>
            <groupId>org.jboss.as</groupId>
            <artifactId>jboss-as-dist</artifactId>
            <version>${version.org.jboss.as.dist}</version>
            <type>zip</type>
          </dependency>
        </dependencies>
      </dependencyManagement>

      <build>
        <plugins>
          <!-- 1. install server in the build/target dir -->
          <plugin>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack-jboss</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.jboss.as</groupId>
                      <artifactId>jboss-as-dist</artifactId>
                      <version>${version.org.jboss.as.dist}</version>
                      <type>zip</type>
                      <overWrite>false</overWrite>
                      <outputDirectory>${project.build.directory}</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
              <execution>
                <id>copy-byteman-agent</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>copy</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.jboss.byteman</groupId>
                      <artifactId>byteman</artifactId>
                      <version>${version.org.jboss.byteman}</version>
                      <type>jar</type>
                      <overWrite>true</overWrite>
                      <outputDirectory>target/</outputDirectory>
                      <destFileName>byteman.jar</destFileName>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
              <execution>
                <id>get-eap-kie-wb-war</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>copy</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>${deployable.groupId}</groupId>
                      <artifactId>${deployable.artifactId}</artifactId>
                      <classifier>${eap64.deployable.classifier}</classifier>
                      <type>war</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                  </artifactItems>
                  <outputDirectory>${project.build.directory}/wars</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 2. install files with user/password information in the server dir -->
          <plugin>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-user-config</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.directory}/${jboss.server.name}/standalone/configuration</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/eap-wildfly-shared/config</directory>
                      <filtering>false</filtering>
                    </resource>
                  </resources>
                  <overwrite>true</overwrite>
                </configuration>
              </execution>
              <execution>
                <id>copy-ssl-files</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.directory}/${jboss.server.name}/standalone/configuration/ssl</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/ssl/server</directory>
                      <filtering>false</filtering>
                    </resource>
                  </resources>
                  <overwrite>true</overwrite>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 3. modify server configuration -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>xml-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>modify-standalone-full</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>transform</goal>
                </goals>
                <configuration>
                  <transformationSets>
                    <transformationSet>
                      <dir>${project.build.directory}/${jboss.server.name}/standalone/configuration</dir>
                      <outputDir>${project.build.directory}/${jboss.server.name}/standalone/configuration</outputDir>
                      <includes>
                        <include>standalone-full.xml</include>
                      </includes>
                      <stylesheet>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/eap6/xslt/addDebugLogging.xslt</stylesheet>
                    </transformationSet>
                    <transformationSet>
                      <dir>${project.build.directory}/${jboss.server.name}/standalone/configuration</dir>
                      <outputDir>${project.build.directory}/${jboss.server.name}/standalone/configuration</outputDir>
                      <includes>
                        <include>standalone-full.xml</include>
                      </includes>
                      <stylesheet>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/eap6/xslt/changeJmsMessagingConfig.xslt</stylesheet>
                    </transformationSet>
                  </transformationSets>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 4. clean up -->
          <plugin>
            <groupId>com.google.code.maven-replacer-plugin</groupId>
            <artifactId>replacer</artifactId>
            <executions>
              <execution>
                <id>fix-broken-junit-reports</id>
                <phase>post-integration-test</phase>
                <goals>
                  <goal>replace</goal>
                </goals>
                <configuration>
                  <basedir>${project.build.directory}/failsafe-reports</basedir>
                  <includes>
                    <include>TEST*xml</include>
                  </includes>
                  <unescape>true</unescape>
                  <tokenValueMap>${basedir}/replace-tokenFile.txt</tokenValueMap>
                  <ignoreErrors>true</ignoreErrors>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>arquillian-tomcat8x</id>

      <properties>
        <tomcat.home>${project.build.directory}/apache-tomcat-${version.org.apache.tomcat}</tomcat.home>
        <deployable.classifier>${tomcat7.deployable.classifier}</deployable.classifier>

        <arquillian.launch>tomcat</arquillian.launch>
      </properties>

      <dependencies>
        <!-- wars -->
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-wb-distribution-wars</artifactId>
          <classifier>tomcat7</classifier>
          <scope>test</scope>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-drools-wb-distribution-wars</artifactId>
          <classifier>tomcat7</classifier>
          <scope>test</scope>
          <type>war</type>
        </dependency>

        <!-- arquillian -->
        <dependency>
          <groupId>org.jboss.arquillian.container</groupId>
          <artifactId>arquillian-tomcat-managed-7</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <!-- ARQ-1456 -->
          <groupId>commons-codec</groupId>
          <artifactId>commons-codec</artifactId>
          <scope>test</scope>
        </dependency>

        <!-- server -->
        <dependency>
          <groupId>org.apache.tomcat</groupId>
          <artifactId>tomcat</artifactId>
          <type>zip</type>
          <scope>test</scope>
        </dependency>

        <!-- deps needed by Tomcat 7x and Tomcat 8x -->
        <!-- :persistence -->
        <dependency>
          <groupId>org.codehaus.btm</groupId>
          <artifactId>btm</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.codehaus.btm</groupId>
          <artifactId>btm-tomcat55-lifecycle</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>com.h2database</groupId>
          <artifactId>h2</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.jboss.spec.javax.transaction</groupId>
          <artifactId>jboss-transaction-api_1.1_spec</artifactId>
          <scope>test</scope>
        </dependency>
        <!-- :logging -->
        <dependency>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-jdk14</artifactId>
        </dependency>
        <!-- :integration -->
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-tomcat-integration</artifactId>
          <scope>test</scope>
        </dependency>
        <!-- :JACC -->
        <dependency>
          <groupId>org.jboss.spec.javax.security.jacc</groupId>
          <artifactId>jboss-jacc-api_1.4_spec</artifactId>
          <scope>test</scope>
        </dependency>
      </dependencies>

      <dependencyManagement>
        <dependencies>
          <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>tomcat</artifactId>
            <version>${version.org.apache.tomcat}</version>
            <scope>test</scope>
          </dependency>
        </dependencies>
      </dependencyManagement>

      <build>
        <plugins>

          <!-- 1. unpack tomcat, grab war and install jars needed for tomcat-->
          <plugin>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack-tomcat</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.apache.tomcat</groupId>
                      <artifactId>tomcat</artifactId>
                      <version>${version.org.apache.tomcat}</version>
                      <type>zip</type>
                      <overWrite>false</overWrite>
                      <outputDirectory>${project.build.directory}</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
              <execution>
                <id>get-tomcat-kie-wb-war</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>copy</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.kie</groupId>
                      <artifactId>kie-wb-distribution-wars</artifactId>
                      <classifier>tomcat7</classifier>
                      <type>war</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                  </artifactItems>
                  <outputDirectory>${project.build.directory}/wars</outputDirectory>
                </configuration>
              </execution>
              <execution>
                <id>copy-kie-wb-libs</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>copy</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <!-- peristence -->
                    <artifactItem>
                      <groupId>org.codehaus.btm</groupId>
                      <artifactId>btm</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <artifactItem>
                      <groupId>org.codehaus.btm</groupId>
                      <artifactId>btm-tomcat55-lifecycle</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <artifactItem>
                      <groupId>com.h2database</groupId>
                      <artifactId>h2</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <artifactItem>
                      <groupId>org.jboss.spec.javax.transaction</groupId>
                      <artifactId>jboss-transaction-api_1.1_spec</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <!-- logging -->
                    <artifactItem>
                      <groupId>org.slf4j</groupId>
                      <artifactId>slf4j-api</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <artifactItem>
                      <groupId>org.slf4j</groupId>
                      <artifactId>slf4j-jdk14</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <!-- integration -->
                    <artifactItem>
                      <groupId>org.kie</groupId>
                      <artifactId>kie-tomcat-integration</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                    <!-- JACC -->
                    <artifactItem>
                      <groupId>org.jboss.spec.javax.security.jacc</groupId>
                      <artifactId>jboss-jacc-api_1.4_spec</artifactId>
                      <type>jar</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                  </artifactItems>
                  <outputDirectory>${tomcat.home}/lib</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 2. copy configuration files (users, etc) -->
          <plugin>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-resources</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${tomcat.home}/conf</outputDirectory>
                  <overwrite>true</overwrite>
                  <resources>
                    <resource>
                      <!-- so that files with (maven) filtered values are used -->
                      <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/filtered-resources/tomcat-shared/config</directory>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 3. modify tomcat server.xml  -->
          <plugin>
            <groupId>com.google.code.maven-replacer-plugin</groupId>
            <artifactId>replacer</artifactId>
            <executions>
              <execution>
                <id>fix-server-xml</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>replace</goal>
                </goals>
                <configuration>
                  <basedir>${tomcat.home}/conf</basedir>
                  <includes>
                    <include>server.xml</include>
                  </includes>
                  <replacements>
                    <replacement>
                      <token>  &lt;/Host&gt;</token>
                      <value>&lt;Valve className="org.kie.integration.tomcat.JACCValve" /&gt;
                        &lt;/Host&gt;</value>
                    </replacement>
                  </replacements>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 4. delete the ROOT app -->
          <plugin>
            <artifactId>maven-antrun-plugin</artifactId>
            <executions>
              <execution>
                <phase>generate-test-resources</phase>
                <configuration>
                  <tasks>
                    <delete dir="${project.build.directory}/tomcat-${version.org.apache.tomcat}/webapps/ROOT"/>
                  </tasks>
                </configuration>
                <goals>
                  <goal>run</goal>
                </goals>
              </execution>
            </executions>
          </plugin>

          <!-- 5. run tests -->
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <environmentVariables>
                <CATALINA_HOME>${tomcat.home}</CATALINA_HOME>
              </environmentVariables>
              <!-- Workaround for SHRINKDESC-97 until it is fixed. -->
              <argLine>-Dfile.encoding=${project.build.sourceEncoding}</argLine>
            </configuration>
          </plugin>
        </plugins>
      </build>

    </profile>

    <profile>
      <id>arquillian-wildfly8x</id>

      <properties>
        <wildfly.server.name>wildfly-${version.wildfly8}</wildfly.server.name>
        <deployable.classifier>${wildfly8.deployable.classifier}</deployable.classifier>

        <arquillian.launch>wildfly</arquillian.launch>
      </properties>

      <dependencies>
        <!-- wars -->
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-wb-distribution-wars</artifactId>
          <classifier>${wildfly8.deployable.classifier}</classifier>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-drools-wb-distribution-wars</artifactId>
          <classifier>${wildfly8.deployable.classifier}</classifier>
          <type>war</type>
        </dependency>

        <!-- arquillian wildfly -->
        <dependency>
          <groupId>org.wildfly</groupId>
          <artifactId>wildfly-arquillian-container-managed</artifactId>
          <scope>test</scope>
        </dependency>

        <!-- server -->
        <dependency>
          <groupId>org.wildfly</groupId>
          <artifactId>wildfly-dist</artifactId>
          <type>zip</type>
        </dependency>
      </dependencies>

      <dependencyManagement>
        <dependencies>
          <dependency>
            <groupId>org.wildfly</groupId>
            <artifactId>wildfly-dist</artifactId>
            <version>${version.wildfly8}</version>
            <scope>test</scope>
            <type>zip</type>
          </dependency>
          <dependency>
            <groupId>org.wildfly</groupId>
            <artifactId>wildfly-arquillian-container-managed</artifactId>
            <version>${version.wildfly8}</version>
            <scope>test</scope>
          </dependency>
        </dependencies>
      </dependencyManagement>

      <build>
        <!-- running tests -->
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>reserve-network-ports</id>
                <goals>
                  <goal>reserve-network-port</goal>
                </goals>
                <phase>initialize</phase>
                <configuration>
                  <portNames>
                    <portName>wildfly.server.port</portName>
                    <portName>wildfly.mgmt.server.port</portName>
                  </portNames>
                  <outputFile>src/test/resources/port.properties</outputFile>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- test configuration -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <systemPropertyVariables>
                <jboss.server.port>${wildfly.server.port}</jboss.server.port>
                <jboss.mgmt.server.port>${wildfly.mgmt.server.port}</jboss.mgmt.server.port>
              </systemPropertyVariables>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <configuration>
              <argLine>-Xms512m -Xmx1024m -XX:MaxPermSize=512m</argLine>
              <systemPropertyVariables>
                <jboss.server.port>${jboss.server.port}</jboss.server.port>
                <jboss.mgmt.server.port>${jboss.mgmt.server.port}</jboss.mgmt.server.port>
                <arquillian.launch>wildfly8</arquillian.launch>
              </systemPropertyVariables>
            </configuration>
          </plugin>

          <!-- 1. install server in the build/target dir -->
          <plugin>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack-wildfly-10</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.wildfly</groupId>
                      <artifactId>wildfly-dist</artifactId>
                      <version>${version.wildfly8}</version>
                      <type>zip</type>
                      <overWrite>false</overWrite>
                      <outputDirectory>${project.build.directory}</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
              <execution>
                <id>get-wildfly-8-kie-wb-war</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>copy</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.kie</groupId>
                      <artifactId>kie-wb-distribution-wars</artifactId>
                      <classifier>${wildfly8.deployable.classifier}</classifier>
                      <type>war</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                  </artifactItems>
                  <outputDirectory>${project.build.directory}/wars</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 2. install files with user/password information in the server dir -->
          <plugin>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-user-config</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.directory}/${wildfly.server.name}/standalone/configuration</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/eap-wildfly-shared/config</directory>
                      <filtering>false</filtering>
                    </resource>
                  </resources>
                  <overwrite>true</overwrite>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 3. modify server configuration -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>xml-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>modify-standalone-full</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>transform</goal>
                </goals>
                <configuration>
                  <transformationSets>
                    <transformationSet>
                      <dir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</dir>
                      <outputDir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</outputDir>
                      <includes>
                        <include>standalone-full.xml</include>
                      </includes>
                      <stylesheet>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/wildfly8/xslt/addDebugLogging.xslt</stylesheet>
                    </transformationSet>
                    <transformationSet>
                      <dir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</dir>
                      <outputDir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</outputDir>
                      <includes>
                        <include>standalone-full.xml</include>
                      </includes>
                      <stylesheet>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/wildfly8/xslt/changeJmsMessagingConfig.xslt</stylesheet>
                    </transformationSet>
                  </transformationSets>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>arquillian-wildfly10x</id>

      <properties>
        <wildfly.server.name>wildfly-${version.wildfly10}</wildfly.server.name>
        <deployable.classifier>${wildfly10.deployable.classifier}</deployable.classifier>
        <arquillian.launch>wildfly</arquillian.launch>
      </properties>

      <dependencies>
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-wb-distribution-wars</artifactId>
          <classifier>${wildfly10.deployable.classifier}</classifier>
          <type>war</type>
        </dependency>
        <dependency>
          <groupId>org.kie</groupId>
          <artifactId>kie-drools-wb-distribution-wars</artifactId>
          <classifier>${wildfly10.deployable.classifier}</classifier>
          <type>war</type>
        </dependency>

        <dependency>
          <groupId>org.wildfly</groupId>
          <artifactId>wildfly-dist</artifactId>
          <type>zip</type>
        </dependency>
      </dependencies>

      <dependencyManagement>
        <dependencies>
          <dependency>
            <groupId>org.wildfly</groupId>
            <artifactId>wildfly-dist</artifactId>
            <version>${version.wildfly10}</version>
            <type>zip</type>
          </dependency>
          <dependency>
            <groupId>org.wildfly</groupId>
            <artifactId>wildfly-arquillian-container-managed</artifactId>
            <version>${version.wildfly10}</version>
            <scope>test</scope>
          </dependency>
        </dependencies>
      </dependencyManagement>

      <build>
        <!-- running tests -->
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>reserve-network-ports</id>
                <goals>
                  <goal>reserve-network-port</goal>
                </goals>
                <phase>initialize</phase>
                <configuration>
                  <portNames>
                    <portName>wildfly.server.port</portName>
                    <portName>wildfly.mgmt.server.port</portName>
                  </portNames>
                  <outputFile>src/test/resources/port.properties</outputFile>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- test configuration -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <systemPropertyVariables>
                <jboss.server.port>${wildfly.server.port}</jboss.server.port>
                <jboss.mgmt.server.port>${wildfly.mgmt.server.port}</jboss.mgmt.server.port>
              </systemPropertyVariables>
            </configuration>
          </plugin>

          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <configuration>
              <argLine>-Xms512m -Xmx1024m -XX:MaxPermSize=512m</argLine>
              <systemPropertyVariables>
                <jboss.server.port>${wildfly.server.port}</jboss.server.port>
                <jboss.mgmt.server.port>${jv.mgmt.server.port}</jboss.mgmt.server.port>
                <arquillian.launch>wildfly8</arquillian.launch>
              </systemPropertyVariables>
            </configuration>
          </plugin>

          <!-- 1. install server in the build/target dir -->
          <plugin>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>unpack-jboss</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>unpack</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.wildfly</groupId>
                      <artifactId>wildfly-dist</artifactId>
                      <version>${version.wildfly10}</version>
                      <type>zip</type>
                      <overWrite>false</overWrite>
                      <outputDirectory>${project.build.directory}</outputDirectory>
                    </artifactItem>
                  </artifactItems>
                </configuration>
              </execution>
              <execution>
                <id>get-wildfly-10-kie-wb-war</id>
                <phase>generate-test-resources</phase>
                <goals>
                  <goal>copy</goal>
                </goals>
                <configuration>
                  <artifactItems>
                    <artifactItem>
                      <groupId>org.kie</groupId>
                      <artifactId>kie-wb-distribution-wars</artifactId>
                      <classifier>${wildfly10.deployable.classifier}</classifier>
                      <type>war</type>
                      <overWrite>false</overWrite>
                    </artifactItem>
                  </artifactItems>
                  <outputDirectory>${project.build.directory}/wars</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 2. install files with user/password information in the server dir -->
          <plugin>
            <artifactId>maven-resources-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-user-config</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>copy-resources</goal>
                </goals>
                <configuration>
                  <outputDirectory>${project.build.directory}/${wildfly.server.name}/standalone/configuration</outputDirectory>
                  <resources>
                    <resource>
                      <directory>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/eap-wildfly-shared/config</directory>
                      <filtering>false</filtering>
                    </resource>
                  </resources>
                  <overwrite>true</overwrite>
                </configuration>
              </execution>
            </executions>
          </plugin>

          <!-- 3. modify server configuration -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>xml-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>modify-standalone-full</id>
                <phase>process-test-resources</phase>
                <goals>
                  <goal>transform</goal>
                </goals>
                <configuration>
                  <transformationSets>
                    <transformationSet>
                      <dir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</dir>
                      <outputDir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</outputDir>
                      <includes>
                        <include>standalone-full.xml</include>
                      </includes>
                      <stylesheet>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/wildfly-8/xslt/addDebugLogging.xslt</stylesheet>
                    </transformationSet>
                    <transformationSet>
                      <dir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</dir>
                      <outputDir>${project.build.directory}/${wildfly.server.name}/standalone/configuration</outputDir>
                      <includes>
                        <include>standalone-full.xml</include>
                      </includes>
                      <stylesheet>${project.basedir}/../kie-wb-smoke-tests-main/src/test/resources/wildfly-8/xslt/changeJmsMessagingConfig.xslt</stylesheet>
                    </transformationSet>
                  </transformationSets>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

  </profiles>

</project>
