project.version=${project.version}
build.dir=${project.build.directory}
server.keystore.password=${server.keystore.password}
server.truststore.password=${server.truststore.password}
client.keystore.password=${client.keystore.password}
port.offset=${port.offset}
deployable.base.uri=${deployable.base.uri}
