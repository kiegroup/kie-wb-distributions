<!--
    This is the template file for creating a new patch for static modules.

    - Global Input properties
      - modules.source.path
      - modules.output.path
      - patches.source.path
      - scripts.source.path

    - LifeCycle
      - init
        - module-name
        - module-output-path

      - create-module-xml
        - module-name
        - module-output-path
        - module-xml-resources
        - module-xml-dependencies
        - patch-value

      - create-module-assembly-component
        - module-name
        - module-output-path
        - module-target-path
        - assembly-filesets
        - assembly-files
        - assembly-dependencysets-inclusion
        - patch-value

      - finish
        - module-name
        - module-output-path

-->

<project name="static-module-patch-id" xmlns:ac="net.sf.antcontrib" basedir="../../../../">

  <!-- Static modules tasks available for the patch. -->
  <import file="scripts${file.separator}build-static-modules.xml"/>

  <!-- Patch Init lifecycle task.

        Input parameteres:
        - module-name
        - module-output-path
    -->
  <target name="init">

  </target>

  <!-- Patch lifecycle task when creating the module.xml descriptor

        Input parameteres:
        - module-name
        - module-output-path
        - module-xml-resources
        - module-xml-dependencies
        - patch-value
    -->
  <target name="create-module-xml">

  </target>

  <!-- Patch lifecycle task when creating the assembly component descriptor

        Input parameteres:
        - module-name
        - module-output-path
        - module-target-path
        - assembly-filesets
        - assembly-files
        - assembly-dependencysets-inclusion
        - patch-value
    -->
  <target name="create-module-assembly-component">

  </target>

  <!-- Patch finish lifecycle task.

        Input parameteres:
        - module-name
        - module-output-path
    -->
  <target name="finish">

  </target>

</project>