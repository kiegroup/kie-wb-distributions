<!--
    This is the template file for creating a new patch for dynamic modules.

    - Global Input properties
      - modules.source.path
      - modules.output.path
      - patches.source.path
      - scripts.source.path
      - modules.temp.path

    - LifeCycle
      - init
        - module-name
        - module-output-path

      - create-module-assembly
        - module-name
        - module-output-path
        - module-target-path
        - assembly-dependencysets-exclusion
        - assembly-filesets
        - assembly-files
        - patch-value

      - finish
        - module-name
        - module-output-path

-->

<project name="dynamic-module-patch-id" xmlns:ac="net.sf.antcontrib" basedir="../../../../">

  <!-- Dynamic modules tasks available for the patch. -->
  <import file="${scripts.source.path}build-dynamic-module.xml"/>

  <!-- Patch Init lifecycle task.

        Input parameteres:
        - module-name
        - module-output-path
    -->
  <target name="init">

  </target>

  <!-- Patch lifecycle task when creating the assembly descriptor for the dynamic module.

        Input parameteres:
        - module-name
        - module-output-path
        - module-target-path
        - assembly-dependencysets-exclusion
        - assembly-filesets
        - assembly-files
        - patch-value
    -->
  <target name="create-module-assembly">

  </target>

  <!-- Patch finish lifecycle task.

        Input parameteres:
        - module-name
        - module-output-path
    -->
  <target name="finish">

  </target>

</project>